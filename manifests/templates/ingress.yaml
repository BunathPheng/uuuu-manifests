---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uuuu                  # Ingress name
  labels:
    app: uuuu
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "uuuu.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uuuu               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
